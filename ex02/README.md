# Assignment 02
## To Do
- Take the kernel Git tree from Assignment 00 and modify the Makefile to change the `EXTRAVERSION` field. Do it so the running kernel, after modifying, rebuilding, and rebooting, includes "-thor_kernel" in its version string.

## Turn In
- Kernel boot log.
- A patch to the original Makefile, following Linux submission standards (Documentation/SubmittingPatches).
